/* Подробнее к сквозным потокам. Дописывается в window.MORE_FP из more-fp.js. */
Object.assign(window.MORE_FP||(window.MORE_FP={}), {
 "fp-app-boot": "`javac` даёт только `.class`-байткод; jar с манифестом и `Main-Class` собирает `jar`/maven-плагин (у Spring Boot — `repackage`). `java -jar` поднимает JVM, которая делит память на **heap** (`-Xmx`), **стек на поток** (`-Xss`, ~1 МБ) и **Metaspace** под метаданные классов. В контейнере тут первая ошибка: без явного `-Xmx` heap = 25% лимита пода, отсюда «дали 2 ГБ, а падает на 512 МБ». Лечится `-XX:MaxRAMPercentage=75`.\n\nКлассы грузит иерархия загрузчиков (Bootstrap → Platform → **AppClassLoader**) с делегированием родителю; у Spring Boot fat-jar сверху свой `LaunchedURLClassLoader` (в 3.2+ — `LaunchedClassLoader`). Класс проходит **loading → linking (verify, prepare, resolve) → initialization** (`<clinit>`), и всё **лениво**, при первом использовании. Поэтому `NoClassDefFoundError` или конфликт версий библиотеки прилетает не на старте, а через два часа на редкой ветке кода.\n\nДальше **JIT**: сначала интерпретатор, потом tiered compilation — C1 быстро, C2 агрессивно по накопленному профилю. Пока профиль не набран, первые запросы медленнее прогретых в разы, на горячих циклах — на порядок. Скомпилированный код лежит в **code cache** (`ReservedCodeCacheSize`, по умолчанию 240 МБ); переполнил — в логе «CodeCache is full. Compiler has been disabled», и код уезжает в интерпретатор без единого исключения (флашинг может вернуть компиляцию, но деградация уже видна).\n\nКонтекст фреймворка — это `AbstractApplicationContext.refresh()`: скан бинов, `BeanFactoryPostProcessor`, `@PostConstruct`, инициализация **HikariCP**, старт Tomcat. Цену старта видно в Actuator: `application.started.time` и `application.ready.time`; Quarkus часть этой работы делает на сборке и стартует быстрее.\n\n**Где рвётся: readiness отдал OK раньше, чем инстанс держит нагрузку.** `/actuator/health/readiness` становится UP по факту готовности контекста — про JIT, пул и кэши это не говорит ничего: Hikari добивает `minimum-idle` фоном, до `maximum-pool-size` растёт только под нагрузкой, кэши пустые. Балансировщик даёт полную долю трафика, p99 улетает в секунды, вызывающие ретраят по таймауту и добивают холодный инстанс. Лечение: прогрев до публикации, `minimum-idle = maximum-pool-size` и readiness, который реально дёргает зависимости.",
 "fp-request-path": "**nginx** терминирует TLS и, если настроен `$request_id`, проставляет `X-Request-Id`. Его `proxy_read_timeout` (60 с) живёт отдельно от твоих: клиент уже получил **504**, а поток в приложении ещё пилит транзакцию — отсюда «у клиента ошибка, а в БД запись есть».\n\nЗапрос берёт поток Tomcat (`server.tomcat.threads.max=200`) и идёт в `FilterChainProxy` Spring Security — это сервлет-фильтр, то есть **раньше** `DispatcherServlet`: `SecurityContextHolderFilter`, `BearerTokenAuthenticationFilter` (JWT по JWKS), `AuthorizationFilter`. Отказ здесь — 401/403, в логах контроллера пусто.\n\n`DispatcherServlet.doDispatch()`: `RequestMappingHandlerMapping` отдаёт `HandlerMethod` по URL+методу, `RequestMappingHandlerAdapter` собирает аргументы **резолверами** — `RequestResponseBodyMethodProcessor` зовёт Jackson `ObjectMapper.readValue`, `@Valid` идёт в hibernate-validator (`MethodArgumentNotValidException` → 400). Сервис зовётся через **CGLIB-прокси** (`proxyTargetClass=true` по умолчанию): `TransactionInterceptor` открывает транзакцию и биндит ресурс в `ThreadLocal` `TransactionSynchronizationManager`. Сам JDBC-коннект Hibernate берёт **лениво**, на первом запросе, и держит до конца транзакции.\n\nВнутри живёт **persistence context**: `setXxx()` не пишет сразу — Hibernate делает **dirty checking** и `flush` перед коммитом (и перед запросами при `FlushMode.AUTO`), поэтому нарушение constraint прилетает не там, где ты менял поле, и уже в виде `DataIntegrityViolationException` после трансляции Spring. Вызов соседнего метода того же бина идёт мимо прокси — транзакции нет. Ответ сериализует Jackson (`MappingJackson2HttpMessageConverter`) и несёт обратно через фильтры. В **Quarkus** сервлета нет: RESTEasy Reactive выбирает поток по сигнатуре — метод, возвращающий `Uni`/`Multi`, идёт на **event loop** Vert.x, обычный — на worker; `@Blocking`/`@NonBlocking` переопределяют. Заблокировал event loop — просядет весь инстанс, а не один запрос.\n\n**Где рвётся: исчерпан пул соединений.** У Hikari `maximum-pool-size=10`, а томкат-потоков 200 — 190 стоят в `getConnection()` и по `connection-timeout=30000` ловят `SQLTransientConnectionException: Connection is not available, request timed out after 30000ms`. Причина обычно одна: HTTP-вызов **внутри** `@Transactional` — сервис думает 20 с, коннект занят. Смотри `hikaricp_connections_pending`; чинится выносом I/O за транзакцию, таймаутами и circuit breaker.",
 "fp-kafka-path": "Producer шлёт `ProducerRecord(topic, key, value)`, партиция = `murmur2(key) % partitions` — ключ фиксирует порядок **только внутри партиции**, глобального порядка в топике нет. Сообщения копятся в батч (`batch.size=16384`, `linger.ms=0` — под нагрузкой батчинг всё равно есть, за счёт ожидания inflight-ответов) и уходят пачкой. `enable.idempotence=true` и `acks=all` с Kafka 3.0 — дефолты, но их часто переопределяют старым конфигом: с идемпотентностью продюсер получает PID и sequence, и ретрай после сетевого сбоя не даёт дубль в логе.\n\nЛидер партиции дописывает в конец сегмента (append-only, через page cache) и выдаёт **offset**. Гарантия сохранности — это **пара** настроек: `acks=all` на продюсере плюс `min.insync.replicas=2` на топике при RF=3. С `acks=all`, но `min.insync.replicas=1` подтверждение приходит от одного брокера, и падение лидера съедает сообщение. Просел ISR — продюсер ловит `NotEnoughReplicasException`.\n\nКонсьюмеры объединены `group.id`, координатор раздаёт партиции (`CooperativeStickyAssignor` не стопорит всю группу на ребалансе). Одна партиция — один консьюмер в группе, значит **партиции задают потолок параллелизма**: 6 партиций и 10 подов — четыре простаивают. Лаг смотри по `records-lag-max` / `kafka_consumergroup_lag`.\n\nДоставка **at-least-once**, поэтому обработка обязана быть идемпотентной: `enable.auto.commit=false`, `commitSync()` только после успеха, дедуп в БД — unique constraint по `event_id` или inbox. Автокоммит хуже: он подтверждает offset'ы предыдущего `poll()` внутри следующего, то есть может закоммитить то, что ты ещё не дообработал, — при падении это теряется.\n\n**Где рвётся: консьюмер завис на poll.** Тяжёлая обработка не уложилась в `max.poll.interval.ms` (5 минут по умолчанию) — координатор считает консьюмера мёртвым, забирает партиции, другой под перечитывает пачку с последнего коммита (**дубли**), а исходный на `commitSync()` ловит `CommitFailedException`. Важно: heartbeat идёт отдельным потоком (`session.timeout.ms=45000`), так что «жив» и «успевает» — разные проверки. Лечится: `max.poll.records` вниз (500 → 50), тяжёлое в отдельный executor и **DLQ** для poison message — иначе одно битое сообщение блокирует партицию, а лаг растёт.",
 "fp-sql-path": "SQL парсится в дерево и проходит анализ имён: есть ли таблицы и колонки, сходятся ли типы. Для prepared statement этот шаг переиспользуется, а в Postgres после пяти custom-планов планировщик сравнивает их среднюю стоимость с **generic plan** — планом без учёта конкретных значений параметров — и, если тот не хуже, переключается на него (`plan_cache_mode`). Что и сколько раз реально выполнялось — смотри в `pg_stat_statements`.\n\nОптимизатор — **cost-based**: он не знает данных, он их **оценивает** по статистике из `pg_statistic`, собранной `ANALYZE` (`default_statistics_target=100` — сотня бакетов гистограммы). Из оценки кардинальности растёт всё: индекс или Seq Scan, Nested Loop против Hash Join, порядок join'ов. `random_page_cost=4` — наследие HDD; на SSD его ставят в 1.1, иначе планировщик недооценивает индексы.\n\nExecutor идёт по плану: берёт из индекса tid'ы и достаёт строки из heap. Страницы ищутся в **shared_buffers** (дефолт 128 МБ, на выделенном сервере обычно поднимают до ~25% RAM), промах — чтение с диска или из page cache ОС. В `EXPLAIN (ANALYZE, BUFFERS)` это `shared hit` против `read`. **Index Only Scan** включается, только когда visibility map говорит, что страница «вся видима», то есть после vacuum.\n\nВидимость версии строки решает **MVCC** по `xmin`/`xmax` и снапшоту транзакции: UPDATE не правит строку, а пишет новую версию, старая остаётся мусором до `autovacuum`. Долгая `idle in transaction` держит горизонт и запрещает чистку — таблица пухнет (`n_dead_tup`), и точечные запросы читают в разы больше страниц.\n\n**Где рвётся: план уехал после роста таблицы.** На 10 тысячах строк Nested Loop с Index Scan отрабатывал за 5 мс; таблица выросла до 10 миллионов, а `autovacuum_analyze_scale_factor=0.1` требует изменения 10% строк — статистика отстала, планировщик всё ещё ждёт одну строку, и тот же запрос идёт 40 секунд. Диагноз читается прямо в плане: `rows=1` в оценке против `actual rows=250000` и большой `Rows Removed by Filter`. В коде ничего не менялось — лечится `ANALYZE`, поднятием statistics target на колонке и переписыванием запроса под индекс.",
 "fp-deploy-k8s": "`kubectl apply` — это запись **желаемого** состояния. API-сервер валидирует манифест, прогоняет через admission-вебхуки и кладёт в **etcd**; дальше контроллеры приводят реальность к нему: Deployment создаёт **ReplicaSet**, тот — поды. Скорость и безопасность выката задают `maxSurge` и `maxUnavailable` (по 25%): при `maxUnavailable: 0` кластер сначала поднимет новый под и только потом погасит старый.\n\nПланировщик смотрит на `resources.requests` — именно **requests**, а не limits, определяют, влезет ли под на узел. Limits работают потом и по-разному: превышение CPU — это **троттлинг** (JVM просто тормозит, `container_cpu_cfs_throttled_seconds_total`), превышение памяти — **OOMKill** с кодом 137, без стектрейса. Поэтому heap всегда заметно меньше memory limit — вне heap ещё Metaspace, стеки потоков, direct buffers, код JIT.\n\nНа узле kubelet тянет образ по слоям и стартует контейнер. Пробы делят обязанности: **liveness** — «процесс завис, перезапусти», **readiness** — «можно ли слать трафик» (провал убирает под из Endpoints, но не перезапускает), `startupProbe` прикрывает медленный старт JVM и до первого успеха отключает и liveness, и readiness.\n\n**Где рвётся, вариант первый: образ тянется дольше таймаута.** Под приехал на свежий узел без кэша, слой на 800 МБ, реестр далеко — под висит в `ContainerCreating`, ловит `ImagePullBackOff`, а `startupProbe` не спасает: она начинается **после** старта контейнера. Выкат упирается в `progressDeadlineSeconds=600`, `kubectl rollout status` падает с `ProgressDeadlineExceeded`; старые поды живы, простоя нет, но релиз застрял.\n\n**Вариант второй: 502 при гашении старых подов.** Удаление из Endpoints и SIGTERM идут **параллельно** и асинхронно: kube-proxy и Ingress на нодах обновляют правила с задержкой, а процесс уже начал закрываться, и часть уже отправленных запросов приходит в закрывающийся под. Нужен `preStop: sleep 5–10` (дать правилам разъехаться), graceful shutdown приложения и `terminationGracePeriodSeconds` больше самого долгого запроса, иначе SIGKILL прилетит в середине обработки."
});
