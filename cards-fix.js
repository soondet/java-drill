/* Мелкие правки полей карточек: строка «↳ глубже» там, где её не было, и нормализация
 * названий подтем.
 *
 * Поле d необязательное — карточка рендерит его только при наличии. Но оно есть у 778
 * карточек из 794, и шестнадцать без него выбивались из ряда: на них не было короткого
 * уточнения прямо на карточке, только развёрнутое «подробнее» под кнопкой.
 *
 * Формат тот же, что у остальных: одно предложение с нюансом, который чаще всего
 * спрашивают следом, а не пересказ ответа.
 */
(function () {
  var D = {
    "jc-stream-reduce": "reduce требует ассоциативной операции — иначе на parallelStream результат поедет. collect для сборки в коллекцию дешевле: он мутирует контейнер, а не создаёт новый на каждом шаге.",
    "te-mock-stub": "Проверять взаимодействие стоит только там, где оно и есть контракт (письмо ушло, событие опубликовано). Верификация вызовов внутри своего же кода привязывает тест к реализации и ломается на любом рефакторинге.",
    "web-post-201": "Location обязателен именно для 201: без него клиент не знает адрес созданного. Если создание асинхронное — это уже 202 Accepted, и в Location кладут ссылку на статус задачи, а не на ресурс.",
    "jc-catch-order": "Порядок проверяет компилятор, а не рантайм: недостижимый catch — ошибка сборки. Для несвязанных типов порядок не важен, и их лучше объединить в multi-catch через вертикальную черту.",
    "te-captor": "Captor нужен, когда аргумент собирается внутри тестируемого кода. Если значение известно заранее — дешевле обычный matcher в verify. И помни: captor хранит ссылку, а не копию, поэтому изменяемый объект успеет поменяться до проверки.",
    "sd-qps-readwrite": "Средний QPS почти бесполезен сам по себе — считай пик, именно под него закупается железо. И раздели чтение с записью: они масштабируются по-разному, реплики спасают только чтение.",
    "sd-availability-nines": "Девятки считают по времени, доступному пользователю, а не по аптайму процесса. Последовательные зависимости перемножаются: три сервиса по 99.9% дают уже 99.7% на цепочку.",
    "rx-overflow-strategies": "Выбор стратегии — это выбор, что дороже: память, свежесть или полнота. BUFFER без ограничения размера — самый частый способ уронить сервис по памяти; ставь лимит и решай, что делать при его достижении.",
    "grpc-call-types": "Тип вызова задаётся в .proto и потом не меняется без ломки контракта. Двунаправленный поток — не «удобный вариант по умолчанию»: он требует продуманной отмены и таймаутов с обеих сторон.",
    "grpc-schema-evolution": "reserved закрывает номер и имя навсегда — без него удалённый номер однажды переиспользуют, и старый клиент прочитает чужие данные как свои. Менять тип поля нельзя даже при совпадении размера.",
    "grpc-vs-rest": "Главный проигрыш — отладка и совместимость: бинарь не посмотришь в браузере, а из веба нужен прокси grpc-web. Для внешнего публичного API это почти всегда решающий довод в пользу REST.",
    "api-status-codes": "Код статуса — часть контракта, менять его так же больно, как формат тела. 422 против 400 — граница синтаксиса и смысла: разобрали, но правило нарушено. 409 — конфликт состояния, а не входных данных.",
    "api-idempotency": "Ключ идемпотентности хранят вместе с ответом и сроком жизни, иначе повтор через сутки создаст дубль. И ключ должен закрывать всю операцию целиком, а не отдельный шаг внутри неё.",
    "api-versioning": "Версия — это обещание, а не номер: главное решить заранее, сколько версий держите одновременно и как объявляете вывод старой из эксплуатации. Без этого любой способ версионирования превращается в вечную поддержку всех версий сразу.",
    "api-problem-json": "Стандарт ценен не полями, а тем, что клиент пишет один обработчик ошибок на все сервисы. Поле type — это стабильный идентификатор класса ошибки, по нему клиент и ветвится; detail предназначен человеку и меняться может свободно.",
    "api-resources-verbs": "Действия вне CRUD моделируют ресурсом-состоянием или подресурсом: не POST /orders/1/cancel, а POST /orders/1/cancellation. Вложенность глубже двух уровней почти всегда означает, что внутри спрятана самостоятельная сущность."
  };
  /* Раньше здесь склеивались подтемы, различающиеся только регистром: «Generics» и
     «generics» выглядели дублем в фильтре. Оказалось, это не дубль, а осознанное
     разделение: у каждой формы своя запись в карте книг со своим обоснованием, а у
     Java / Strings и Testing / mockito ещё и разные книги. Склейка молча подменяла
     источник у семи карточек, поэтому нормализация убрана. Если две ветки в фильтре
     мешают — их надо переименовать в разные слова вместе с картой книг, а не сливать. */
  var S = {};
  var list = (window.CARDS || []).concat(window.CARDS_EXTRA || [], window.CARDS_NEW || []);
  var n = 0, m = 0;
  list.forEach(function (c) {
    if (!c) return;
    if (D[c.id] && !String(c.d || "").trim()) { c.d = D[c.id]; n++; }
    if (c.s && S[c.s]) { c.s = S[c.s]; m++; }
  });

  /* У 16 добавленных карточек не было двух вещей, которые есть у всех остальных:
     короткой заметки (NOTES) и образной зацепки (HOOKS). Без них карточка выглядит
     иначе, чем соседние, — а зацепка тут половина метода. */
  var N = {
    "gp2-load-what-to-measure": "Перцентили разных инстансов нельзя усреднять: p99 по кластеру считают из гистограммных корзин, а не из среднего p99 подов.",
    "gp2-load-mistakes": "Инструменты, держащие заданный темп независимо от ответов (wrk2, Gatling), лечат coordinated omission; ab и обычный wrk — нет.",
    "gp2-scoped-value": "Дочерние задачи внутри StructuredTaskScope видят связанное значение автоматически — отдельно пробрасывать не нужно.",
    "gp2-record-patterns": "Добавили компонент в запись — все шаблоны с фиксированным числом переменных перестанут компилироваться. Это и хорошо: покажет все места разбора.",
    "gp2-switch-exhaustive": "null в switch по типам не совпадает ни с одним шаблоном и даёт NPE: нужна явная ветка case null.",
    "gp2-sql-window-rank": "Порядок внутри равных у row_number не определён — для устойчивой пагинации в ORDER BY добавляют уникальный ключ.",
    "gp2-sql-window-frame": "ROWS 6 PRECEDING — это семь строк, а не семь дней: при пропусках в данных календарное окно так не посчитать.",
    "gp2-log-levels": "Одно исключение, залогированное и проброшенное выше, даёт три записи ERROR в разных местах — логируй там, где обрабатываешь.",
    "gp2-log-mdc": "Через CompletableFuture и реактивные операторы MDC не переезжает: контекст копируют в обёртке задачи.",
    "gp2-regex-backtracking": "Проверять шаблон надо на строках, которые почти совпадают: катастрофа наступает там, где совпадения нет.",
    "gp2-time-types": "Duration и Period не взаимозаменяемы: сутки не всегда 24 часа, и при переходе на летнее время это расходится.",
    "gp2-time-utc-trap": "Устаревшая база часовых поясов в старом образе даёт неверное время после очередной реформы — tzdata обновляют вместе с образом.",
    "gp2-chaos": "Первый эксперимент ставят не на проде: на стенде сразу видно, замечает ли мониторинг отказ вообще.",
    "gp2-contract-compat": "Худшее изменение — то же имя поля с новым смыслом: его не ловит ни схема, ни компилятор, ни тест контракта.",
    "gp2-feature-flags": "Десяток флагов даёт комбинаторику, которую целиком никто не проверяет: тестируют обычно только включённый путь.",
    "gp2-reactive-when": "Один блокирующий вызов внутри цепочки занимает поток event-loop целиком и роняет обслуживание всех запросов сразу."
  };
  var HK = {
    "gp2-load-what-to-measure": "Марафон: важна не только скорость, но и пульс с числом сошедших. Один результат без второго и третьего ничего не говорит.",
    "gp2-load-mistakes": "Тест-драйв по пустой парковке. Машина отличная — пока не выедешь в час пик на настоящую дорогу.",
    "gp2-scoped-value": "Бейдж гостя на входе: действует, пока ты внутри, и забирается на выходе сам. Пропуск сотрудника надо помнить сдать.",
    "gp2-record-patterns": "Посылку не носят целиком: вскрыл — и сразу достал то, что внутри, не спрашивая курьера про каждый предмет.",
    "gp2-switch-exhaustive": "Список дел, где галочки обязаны стоять напротив всех пунктов. Добавили пункт — список сам напомнит, что он пустой.",
    "gp2-sql-window-rank": "Пьедестал: два вторых места. rank оставит третье пустым, dense_rank — нет, а row_number просто раздаст 1-2-3.",
    "gp2-sql-window-frame": "Окно вагона: ROWS — «пять сидений подряд», RANGE — «все, кто вышел на этой станции», а их может быть и десять.",
    "gp2-log-levels": "Пожарная сигнализация. Если она воет на каждый тост, на настоящий пожар уже никто не побежит.",
    "gp2-log-mdc": "Номерок в гардеробе на всех вещах сразу. Пересел в другой поток — номерок не переехал, вещи стали ничьи.",
    "gp2-regex-backtracking": "Подбор ключа к чужой двери: пока один подходит — быстро. Когда не подходит ни один, перебираешь всю связку целиком.",
    "gp2-time-types": "«В 15:00» и «через три часа» — разное. Первое зависит от того, где ты стоишь, второе — нет.",
    "gp2-time-utc-trap": "Билет на поезд «в 14:00 по местному». Переведут часы в стране — время в билете останется, а момент отправления сдвинется.",
    "gp2-chaos": "Пожарные учения. Смысл не в том, чтобы что-то сгорело, а в том, чтобы выяснить, работает ли сигнализация и знают ли люди выход.",
    "gp2-contract-compat": "Розетка: добавить рядом ещё одну можно, а переставить контакты в старой — и вся техника в доме перестанет включаться.",
    "gp2-feature-flags": "Строительные леса. Пока идёт стройка — нужны, после сдачи дома — просто мешают и однажды на кого-то падают.",
    "gp2-reactive-when": "Официант против конвейера. Пока гости ждут заказ, официант обслуживает других; но овощи он всё равно режет своими руками."
  };
  window.NOTES = window.NOTES || {};
  window.HOOKS = window.HOOKS || {}; window.HOOKS.cards = window.HOOKS.cards || {};
  var nn = 0;
  Object.keys(N).forEach(function (k) { if (!window.NOTES[k]) { window.NOTES[k] = N[k]; nn++; } });
  Object.keys(HK).forEach(function (k) { if (!window.HOOKS.cards[k]) window.HOOKS.cards[k] = HK[k]; });

  /* Новым подтемам не хватало источника: без записи карточка откатывалась на книгу
     всей темы, а «Release It!» и SRE подходят точнее, чем общий учебник по тестам. */
  if (window.BOOKS && window.BOOKS.map) {
    var BM = window.BOOKS.map;
    if (!BM["Testing / нагрузка"]) BM["Testing / нагрузка"] = { book: "release-it", why: "нагрузка, запас мощности и поведение под пиком" };
    if (!BM["DevOps / reliability"]) BM["DevOps / reliability"] = { book: "sre", why: "проверка отказов и бюджет ошибок" };
  }

  /* Structured Concurrency учится по форме API из Java 21 (ShutdownOnFailure,
     throwIfFailed). После 21 API переработали, и старые классы убрали, поэтому
     карточка без пометки готовит к ответу про несуществующие сейчас классы.
     Сам концепт — дерево задач и отмена — не изменился, его и надо рассказывать. */
  var SC = "gap-sc-structured-task-scope";
  var scNote = "Форма API менялась: в Java 21 это было превью с ShutdownOnFailure/ShutdownOnSuccess и throwIfFailed(). "
    + "Позже scope стали открывать через StructuredTaskScope.open(...) с политикой Joiner, а join() сам возвращает результат — "
    + "старых подклассов больше нет. ScopedValue при этом доведён до финального API. На собесе рассказывай идею дерева задач "
    + "и отмены, а не сигнатуру: имена классов у разных версий разные, и спрашивающий чаще помнит именно идею.";
  (function () {
    var c = list.filter(function (x) { return x && x.id === SC; })[0];
    if (c) c.d = (c.d ? c.d + " " : "") + scNote;
    if (window.MORE && window.MORE[SC]) window.MORE[SC] = "**Версии API.** " + scNote + "\n\n" + window.MORE[SC];
  })();
  if (typeof console !== "undefined" && console.debug)
    console.debug("cards-fix: «глубже» " + n + ", заметок " + nn);
})();
