/* Ревью: «вот изменение — какое замечание здесь блокирующее».
 *
 * Отличие от «найди баг». Там надо найти дефект в коде, и он там точно есть. Здесь
 * задача другая и более сеньорская: в изменении обычно видно сразу несколько поводов
 * что-то сказать, и надо отделить то, из-за чего нельзя вливать, от того, что стоит
 * обсудить, и от вкусовщины, на которой ревью буксует чаще всего.
 *
 * Поэтому в каждом наборе вариантов лежит: одно настоящее блокирующее замечание,
 * одно верное по сути, но не блокирующее, одна вкусовщина и одно замечание, которое
 * просто неверно. Все четыре — то, что реально пишут в комментариях.
 *
 * Схема: {id, t:кластер, ctx:что за изменение, code, correct, wrong:[3], why:разбор}
 */
window.REVIEW = [

{
  id: "rv-tx-external-call", t: "Spring",
  ctx: "Добавили отправку уведомления при создании заказа.",
  code: "@Transactional\npublic Order create(OrderDto dto) {\n  Order o = repo.save(Order.from(dto));\n  notifier.send(o.getClientId(), \"заказ создан\");\n  audit.log(\"created\", o.getId());\n  return o;\n}",
  correct: "Сетевой вызов внутри транзакции держит соединение к базе всё время ответа notifier",
  wrong: ["Метод стоит переименовать в createOrder — так понятнее из вызывающего кода, что именно создаётся",
          "Строку «заказ создан» надо вынести в константу или ресурсный файл, а не хардкодить прямо в методе",
          "@Transactional на публичном методе не работает — нужен отдельный сервис-прокси, иначе аннотация игнорируется"],
  why: "Транзакция открыта, внутри неё синхронный поход наружу. Соединение к базе занято ровно столько, сколько отвечает notifier, — при его замедлении кончится пул, и ляжет всё приложение, а не только уведомления.\n\nЭто и есть блокирующее: последствие не в этом методе, а в соседних.\n\nПереименование и константа — верные по-своему, но обсуждаемые: они не мешают влить. А вот утверждение про «@Transactional не работает на публичном методе» просто неверно: не работает при вызове изнутри того же класса, а публичность как раз обязательна."
},
{
  id: "rv-equals-no-hashcode", t: "Java",
  ctx: "Добавили сравнение по бизнес-ключу.",
  code: "public class Account {\n  private final String iban;\n  @Override public boolean equals(Object o) {\n    return o instanceof Account a\n        && a.iban.equals(iban);\n  }\n}",
  correct: "Переопределён equals без hashCode — объект потеряется в HashMap и HashSet",
  wrong: ["Стоит добавить проверку на null у поля iban перед вызовом equals на нём",
          "Лучше использовать Objects.equals вместо прямого вызова — так короче и привычнее читателю",
          "Класс надо пометить final, иначе наследник сможет нарушить контракт симметричности equals"],
  why: "Контракт требует: равные объекты обязаны давать равный hashCode. Без hashCode два равных счёта попадут в разные корзины, и HashSet будет считать их разными, а get по ключу вернёт null. Ошибка тихая, вылезет далеко от этого класса.\n\nОстальные три замечания верны в разной степени. Про null — уместно, если iban может быть пустым. Objects.equals — вкусовщина. Про final и симметричность — правильное теоретически замечание, но оно не про поломку здесь и сейчас, а про потенциального наследника, которого ещё нет."
},
{
  id: "rv-catch-swallow", t: "Java",
  ctx: "Обернули вызов внешнего сервиса, чтобы не падать.",
  code: "try {\n  rates = client.fetchRates();\n} catch (Exception e) {\n  log.warn(\"не получили курсы\");\n  rates = Collections.emptyMap();\n}",
  correct: "Пустая карта вместо курсов уйдёт дальше как валидные данные, и расчёт молча посчитает по нулям",
  wrong: ["В лог надо писать сам объект исключения, а не только текст — иначе стектрейс потерян",
          "Ловить Exception слишком широко: стоит перечислить конкретные типы, которые действительно ожидаются",
          "log.warn здесь занижен, такое надо писать на уровне error, чтобы алерт сработал"],
  why: "Все четыре замечания верны, и в этом сложность. Но три из них про качество разбора, а одно — про деньги: пустая карта неотличима от «курсов нет вообще», и следующий по цепочке посчитает сумму по нулевому курсу. Это блокирует.\n\nПотерянный стектрейс — самое полезное из остальных, и его точно стоит написать, но чинится он в любой момент. Ширина catch и уровень лога — обсуждаемое.\n\nПравильная починка не в логировании, а в решении: либо падать, либо возвращать явное «данных нет», которое вызывающий обязан обработать."
},
{
  id: "rv-nplus1", t: "Spring",
  ctx: "Сделали выгрузку заказов с позициями.",
  code: "List<Order> orders = repo.findAll();\nreturn orders.stream()\n  .map(o -> new OrderView(\n      o.getId(),\n      o.getItems().size()))\n  .toList();",
  correct: "getItems() у ленивой связи даст отдельный запрос на каждый заказ — при тысяче заказов это тысяча запросов",
  wrong: ["findAll() без пагинации вытащит всю таблицу в память — не хватает Pageable",
          "Маппинг прямо в потоке читается плохо: ему место в отдельном методе или маппере",
          "toList() возвращает неизменяемый список — надо collect(Collectors.toList()), если его будут менять"],
  why: "Здесь два настоящих замечания, и выбирать надо по последствиям. N+1 превращает один запрос в тысячу и роняет базу под нагрузкой — это блокирует.\n\nОтсутствие пагинации — тоже серьёзно и его надо написать, но оно бьёт по памяти одного пода предсказуемым образом, а N+1 бьёт по общей базе и по всем соседям. Если выбирать одно, чтобы не влить — это N+1.\n\nМаппинг в отдельный метод — вкусовщина. Про toList() замечание техническое, но здесь список никто не меняет, значит повод для разговора, а не для блокировки."
},
{
  id: "rv-secret-in-code", t: "Web",
  ctx: "Добавили интеграцию с платёжным шлюзом.",
  code: "private static final String API_KEY =\n    \"sk_live_51H8xQe2eZvKY\";\n\npublic PayResp charge(long sum) {\n  return http.post(URL, sum, API_KEY);\n}",
  correct: "Боевой ключ в исходниках — он уже в истории git и его придётся отзывать, а не просто удалять",
  wrong: ["Ключ надо вынести в конфигурацию, чтобы его можно было менять без пересборки приложения",
          "Сумма передаётся как long, а не BigDecimal — для денег примитив опасен округлением",
          "Вокруг http.post нет обработки ошибок: сейчас исключение уйдёт наверх как есть"],
  why: "Первый и второй варианты выглядят почти одинаково, и разница между ними — суть замечания. «Вынести в конфиг» описывает правильную архитектуру. «Ключ уже утёк» описывает то, что нужно сделать прямо сейчас: удаление строки из кода не убирает её из истории, ключ надо отзывать и выпускать заново.\n\nЭто именно то, что отличает полезное ревью: не «как надо было», а «что теперь делать».\n\nПро BigDecimal — верно по существу и стоит обсудить отдельно, но это не блокирует. Обработка ошибок — обсуждаемое: наверх может ловить общий обработчик."
},
{
  id: "rv-mutable-static", t: "Concurrency",
  ctx: "Добавили счётчик обработанных сообщений.",
  code: "public class Handler {\n  private static int processed = 0;\n\n  public void handle(Message m) {\n    processed++;\n    doWork(m);\n  }\n}",
  correct: "Инкремент общего статического поля из нескольких потоков теряет часть значений",
  wrong: ["Счётчик живёт в поле, а не в метриках Micrometer — снаружи его никто никогда не увидит",
          "Поле надо объявить volatile, чтобы изменения счётчика были видны другим потокам сразу",
          "static-поле мешает тестам: между тестовыми методами значение счётчика не сбрасывается"],
  why: "processed++ — это три операции: прочитать, прибавить, записать. Два потока успевают вклиниться между ними, и часть инкрементов теряется. Счётчик врёт, причём тем сильнее, чем выше нагрузка.\n\nВариант с volatile — самая частая ошибка на ревью: volatile обеспечивает видимость, но не атомарность, и потери останутся. Нужен AtomicInteger или счётчик метрик.\n\nЗамечание про Micrometer верное и по делу — скорее всего, так и надо. Про тесты тоже верное. Но обе проблемы решаются спокойно, а неверный счётчик уже врёт в отчёте."
},
{
  id: "rv-missing-idempotency", t: "Distributed",
  ctx: "Обработчик события из очереди.",
  code: "@KafkaListener(topics = \"payments\")\npublic void onPayment(PaymentEvent e) {\n  balance.add(e.getAccount(), e.getSum());\n  ack.acknowledge();\n}",
  correct: "При повторной доставке сумма прибавится второй раз — обработчик не идемпотентен",
  wrong: ["Нет обработки исключений: при ошибке сообщение зациклится и заблокирует партицию",
          "acknowledge стоит вызывать в finally, чтобы он выполнялся при любом исходе",
          "Имя топика надо вынести в конфигурацию, а не хардкодить в аннотации"],
  why: "Kafka даёт «хотя бы один раз»: повторная доставка — это норма, а не сбой. Обработчик, который на неё не рассчитан, при первом же ребалансе прибавит деньги дважды. Для баланса счёта это блокирует безоговорочно.\n\nЗамечание про зацикливание верное и важное — но оно про доступность, а второе начисление про деньги.\n\nА вот acknowledge в finally — вредный совет, и такой на ревью встречается: подтверждать надо только успешную обработку, иначе сообщение потеряется при ошибке. Имя топика в конфиге — вкусовщина."
},
{
  id: "rv-unbounded-cache", t: "JVM",
  ctx: "Добавили кэш справочника, чтобы не ходить в базу.",
  code: "private final Map<String, Rate> cache =\n    new HashMap<>();\n\npublic Rate get(String code) {\n  return cache.computeIfAbsent(\n      code, this::loadFromDb);\n}",
  correct: "Кэшу стоит задать предел и вытеснение: иначе при потоке ключей он растёт до OOM",
  wrong: ["HashMap не потокобезопасен: под нагрузкой computeIfAbsent из нескольких потоков даст гонку",
          "Самописный кэш на карте вместо готовой библиотеки — лишний велосипед",
          "loadFromDb внутри computeIfAbsent выполняется под блокировкой корзины — это замедлит соседние ключи"],
  why: "Здесь три верных замечания из четырёх, и это ровно та ситуация, ради которой существует расстановка приоритетов.\n\nНеограниченный рост — блокирующее: приложение однажды умрёт по памяти, и произойдёт это на проде под реальным разнообразием ключей.\n\nПотокобезопасность близка по важности, и если ключей заведомо мало, она даже выходит вперёд — на ревью это уместно проговорить вслух. Замечание про загрузку под блокировкой верное и тонкое, обсуждается. Совет взять библиотеку — правильный по существу, но это уже «как чинить», а не «что не так»."
},
{
  id: "rv-string-concat-loop", t: "Java",
  ctx: "Собираем строку для отчёта.",
  code: "String out = \"\";\nfor (Row r : rows) {\n  out += r.getName() + \";\"\n       + r.getSum() + \"\\n\";\n}\nreturn out;",
  correct: "Здесь стоит взять StringBuilder: конкатенация в цикле создаёт новую строку на каждой итерации, на больших отчётах это квадрат",
  wrong: ["Разделитель «;» стоит вынести в константу, чтобы формат менялся в одном месте",
          "Нет экранирования: если в имени встретится точка с запятой, файл поедет по колонкам",
          "Метод делает два дела сразу: собирает строку и форматирует отдельную строку отчёта"],
  why: "На десяти строках разницы нет, на ста тысячах отчёт считается минутами вместо секунд. StringBuilder или Collectors.joining решают это одной строкой.\n\nЗамечание про экранирование при этом самое интересное: оно про корректность данных, а не про скорость, и в отчёте, который уедет клиенту, это может оказаться важнее производительности. На реальном ревью я бы написал оба и явно сказал, какое из них блокирует.\n\nКонстанта и разбиение метода — вкусовщина, на которой ревью буксует чаще всего."
},
{
  id: "rv-test-sleep", t: "Testing",
  ctx: "Тест на асинхронную обработку.",
  code: "service.submit(task);\nThread.sleep(500);\nassertThat(repo.findAll())\n    .hasSize(1);",
  correct: "Ожидание по таймеру делает тест нестабильным: на загруженной машине сборки он падает случайно",
  wrong: ["500 миллисекунд замедляют сборку — при сотне таких тестов набегает минута",
          "assertThat стоит заменить на более точную проверку содержимого, а не только размера",
          "Тест не чистит базу после себя, и следующий тест может увидеть эту запись"],
  why: "Нестабильный тест хуже отсутствующего: он приучает игнорировать красную сборку, и однажды за его миганием пропустят настоящую поломку. Лечится ожиданием условия — Awaitility или явный latch, — а не паузой.\n\nЗамедление сборки — следствие того же дефекта, и его стоит упомянуть, но само по себе оно не блокирует.\n\nПроверка содержимого и очистка базы — верные замечания, которые делают тест лучше. Но именно нестабильность делает его вредным."
},
{
  id: "rv-user-input-query", t: "DB",
  ctx: "Добавили поиск по произвольному полю.",
  code: "String sql = \"select * from users \"\n  + \"where \" + field + \" = '\"\n  + value + \"'\";\nreturn jdbc.queryForList(sql);",
  correct: "Значение подставляется в текст запроса — это SQL-инъекция, читаются и меняются чужие данные",
  wrong: ["Имя поля тоже приходит снаружи — его нельзя параметризовать, нужен белый список допустимых колонок",
          "select * вытащит все колонки, включая те, что не нужны — стоит перечислить явно",
          "queryForList вернёт List<Map>, с которым неудобно работать — лучше замапить в объект"],
  why: "Инъекция блокирует безоговорочно и чинится параметрами запроса.\n\nНо первый неверный вариант тут особенный: он тоже верный и тоже про безопасность. Имя колонки параметром не подставишь, поэтому одними параметрами дыру не закрыть — нужен ещё и белый список. Хорошее ревью назовёт оба; если назвать только параметры, дыра останется наполовину открытой.\n\nselect * и маппинг — обычные обсуждаемые замечания, к безопасности отношения не имеют."
},
{
  id: "rv-optional-get", t: "Java",
  ctx: "Достаём клиента по идентификатору.",
  code: "public String nameOf(long id) {\n  return repo.findById(id)\n      .get()\n      .getName();\n}",
  correct: "get() без проверки бросит NoSuchElementException вместо понятной ошибки «клиент не найден»",
  wrong: ["Стоит использовать orElseThrow с осмысленным исключением, чтобы обработчик отдал корректный статус",
          "Метод возвращает String, а стоило бы Optional<String> — пусть вызывающий решает, что делать с отсутствием",
          "findById тянет всю сущность ради одного name: getReferenceById сэкономил бы запрос"],
  why: "Первый неверный вариант почти совпадает с верным — и это намеренно. Разница в том, что верный называет проблему («будет непонятная ошибка»), а тот называет решение («используй orElseThrow»). На ревью полезнее первое: автор сам выберет способ, а формулировка через последствие не превращается в спор о вкусах.\n\nВозвращать Optional наружу — обсуждаемо и зависит от слоя: контроллеру чаще нужен явный отказ, а не пустота.\n\nПро getReferenceById — техническое замечание, и здесь оно скорее вредно: ленивая ссылка при обращении к полю всё равно сходит в базу, зато может выстрелить вне транзакции."
},
{
  id: "rv-log-pii", t: "DevOps",
  ctx: "Добавили лог для разбора обращений.",
  code: "log.info(\"вход: {} {} {}\",\n    user.getPhone(),\n    user.getPassport(),\n    req.getToken());",
  correct: "Телефон, паспорт и токен уезжают в общее хранилище логов, доступ к которому шире, чем к базе",
  wrong: ["Три отдельных аргумента читаются плохо — лучше собрать в один структурный объект",
          "log.info для каждого входа избыточен: такой объём записей раздует хранилище и удорожит хранение",
          "Токен стоит логировать не целиком, а только первые восемь символов — этого хватит для сопоставления"],
  why: "Персональные данные и токен в логах — это утечка, и блокирует она сразу по двум причинам: доступ к логам обычно есть у большего числа людей, чем к базе, и логи уезжают наружу в системы хранения и анализа.\n\nТретий вариант выглядит как разумный компромисс и часто встречается на ревью, но он про токен и молчит про паспорт — то есть закрывает треть проблемы и создаёт ощущение, что вопрос решён.\n\nЧитаемость и объём логов — обсуждаемое, к утечке отношения не имеет."
},
{
  id: "rv-null-check-late", t: "Java",
  ctx: "Обработка ответа внешнего сервиса.",
  code: "var resp = client.call(req);\nlog.debug(\"ответ: {}\",\n    resp.getBody().length());\nif (resp == null) {\n  return fallback();\n}",
  correct: "Проверка на null стоит после обращения к объекту — до неё выполнение уже не дойдёт",
  wrong: ["Если клиент никогда не возвращает null, проверку надо убрать совсем, а не переставлять",
          "log.debug вычисляет length() даже при выключенном уровне логирования — это лишняя работа",
          "Стоит использовать Optional вместо возврата null из клиента — тогда проверка станет явной"],
  why: "Порядок здесь делает и проверку, и лог бессмысленными: если resp окажется null, падение произойдёт на строке с логом, а до fallback дело не дойдёт. Причём в логах это будет выглядеть как ошибка логирования, что уводит от причины.\n\nПервый неверный вариант верен по сути — мёртвая проверка действительно должна либо работать, либо исчезнуть, — но начинать надо с того, что код сейчас падает.\n\nПро вычисление length() замечание точное и полезное: аргументы вычисляются до вызова независимо от уровня. Optional — правильное направление, но это переделка контракта клиента."
},
{
  id: "rv-migration-drop", t: "DB",
  ctx: "Миграция вместе с релизом, выкат rolling.",
  code: "-- V12__cleanup.sql\nALTER TABLE orders\n  DROP COLUMN legacy_status;",
  correct: "При постепенном выкате старые поды ещё читают колонку и начнут падать сразу после миграции",
  wrong: ["Удаление колонки необратимо: данные пропадут без возможности восстановить их обратной миграцией",
          "Миграцию схемы стоит выполнять в отдельном окне обслуживания, а не вместе с релизом приложения",
          "Имя файла стоит сделать информативнее, чем cleanup — по нему потом будет не понять, что менялось"],
  why: "Rolling-выкат означает, что старая и новая версии какое-то время работают одновременно. Удаление колонки в том же релизе гарантированно ломает старые поды — и, что хуже, ломает откат: вернуть образ можно, вернуть колонку нельзя.\n\nПоэтому такие изменения делают в два релиза: сначала перестаём читать, потом, следующим релизом, удаляем.\n\nПро необратимость — то же самое, но сказанное про данные, а не про доступность; замечание верное и его стоит написать рядом. Окно обслуживания и имя файла — обсуждаемое."
},
{
  id: "rv-thread-pool-unbounded", t: "Concurrency",
  ctx: "Обработка загруженного файла в фоне.",
  code: "ExecutorService pool =\n    Executors.newCachedThreadPool();\n\nrows.forEach(r ->\n    pool.submit(() -> process(r)));",
  correct: "Пул без ограничения создаст поток на каждую строку — миллион строк положит приложение",
  wrong: ["Пул создаётся на каждый вызов метода и нигде не закрывается — это утечка потоков",
          "forEach с побочным эффектом стоит заменить на обычный цикл — так понятнее, что это не преобразование",
          "Нет ожидания завершения: метод вернётся раньше, чем задачи отработают"],
  why: "Здесь два блокирующих замечания, и они связаны. Неограниченный пул создаёт поток под каждую задачу, если свободных нет: на большом файле это тысячи потоков, память и переключения контекста.\n\nНо и первый неверный вариант — настоящая проблема: пул, созданный локально и не закрытый, живёт вечно, и каждый вызов метода добавляет новый. На реальном ревью я бы назвал оба и сказал, что чинятся они вместе — одним пулом с ограничением, поднятым на уровне приложения.\n\nОтсутствие ожидания завершения — верное замечание, зависит от того, нужен ли результат. forEach против цикла — вкусовщина."
},

/* ───── Вторая волна: 24 разбора под сеньорский собес и боевой стек — Kafka, Zeebe,
   Kubernetes, Postgres, MinIO, Keycloak. Место верного ответа по длине разложено заранее,
   по шесть на каждое из четырёх, в русском и в английском. Что можно запустить — запущено:
   у HttpClient нет таймаутов по умолчанию; supplyAsync без пула идёт в общий ForkJoinPool,
   а на двух ядрах в Java 17 — в новый поток на задачу (в 25-й уже нет); Stream.toList() есть
   с 16-й; "ADMIN".equals(null) даёт false; toString по всем полям на двусторонней связи
   кончается StackOverflowError. Остальное — суждения о приоритетах, запуском не проверить. ───── */

{
  id: "rv-retry-storm", t: "Distributed",
  ctx: "Добавили повтор при ошибке вызова сервиса котировок.",
  code: "for (int i = 0; i < 5; i++) {\n  try {\n    return quotes.fetch(isin);\n  } catch (IOException e) {\n    log.warn(\"повтор {}\", i);\n  }\n}\nthrow new QuotesUnavailable();",
  correct: "Повторы идут без паузы и без разброса: когда сервис захлебнётся, все клиенты ударят по нему разом и добьют",
  wrong: ["Пять попыток зашиты числом прямо в коде: без настройки в конфигурации их не поменять без выката новой версии приложения",
          "Цикл for с return внутри читается хуже, чем отдельный метод retry с лямбдой",
          "IOException ловить бесполезно: сетевые ошибки в Java приходят только как RuntimeException"],
  why: "Повтор без задержки превращает чужую проблему в общую. Сервис котировок начал отвечать медленно — каждый клиент тут же шлёт ещё четыре запроса, и нагрузка вырастает впятеро ровно в тот момент, когда он слабее всего. Это лавина повторов, и она блокирует.\n\nЛечится экспоненциальной паузой со случайным разбросом и общим бюджетом на повторы, а надёжнее — предохранителем перед вызовом.\n\nЧисло попыток в конфигурации полезно, но влить не мешает. Форма цикла — вкусовщина. А утверждение про RuntimeException просто неверно: IOException — проверяемое исключение, и сетевые клиенты бросают именно его."
},

{
  id: "rv-ack-before-process", t: "Distributed",
  ctx: "Обработчик платёжных событий с ручным подтверждением.",
  code: "@KafkaListener(topics = \"payouts\")\npublic void on(PayoutEvent e,\n               Acknowledgment ack) {\n  ack.acknowledge();\n  payouts.execute(e);\n}",
  correct: "Подтверждение стоит до обработки: упадём в execute — и событие потеряно навсегда",
  wrong: ["Нет обработки ошибок: стоит хотя бы логировать идентификатор события, на котором упали",
          "Имя метода on неинформативно — лучше onPayoutEvent, чтобы его было видно в стектрейсе",
          "acknowledge() надо звать дважды, до и после, иначе оффсет не сдвинется"],
  why: "Подтверждение говорит брокеру «это я обработал». Сказано до обработки — значит при падении в execute оффсет уже сдвинут, и после рестарта сообщение никто не перечитает. Для выплат это потерянные деньги клиента, и это блокирует.\n\nПорядок должен быть обратным: сначала работа, потом подтверждение. Тогда сбой даёт повторную доставку, а от неё защищает идемпотентность обработчика.\n\nЛогирование идентификатора — верное замечание, но оно про удобство разбора. Имя метода — вкусовщина. Двойной вызов acknowledge — выдумка: подтверждение одно, и его место после успешной обработки."
},

{
  id: "rv-zeebe-job-timeout", t: "Distributed",
  ctx: "Воркер Zeebe формирует отчёт и отправляет его клиенту.",
  code: "@JobWorker(type = \"send-report\",\n           timeout = 30_000)\npublic void handle(ActivatedJob job) {\n  byte[] pdf = reports.build(job); // ~2 мин\n  mail.send(clientOf(job), pdf);\n}",
  correct: "Работа идёт две минуты при таймауте задания в тридцать секунд: брокер отдаст её другому воркеру, и клиент получит несколько писем",
  wrong: ["Отчёт уходит вложением в письме, хотя надёжнее класть его в хранилище и слать ссылку",
          "Константа 30_000 без единиц измерения: Duration читался бы яснее",
          "@JobWorker нельзя ставить на void-метод: без возвращаемого значения задание не завершится"],
  why: "Таймаут задания в Zeebe — это аренда. Воркер взял задание на тридцать секунд; не завершил в срок — брокер считает его пропавшим и отдаёт задание следующему. Тот тоже строит отчёт две минуты, и так по кругу: клиенту уходит несколько одинаковых писем, а процесс может завершиться дважды. Это блокирует.\n\nНужен таймаут с запасом на реальную длительность и идемпотентный обработчик: перед отправкой проверять, что письмо по этому заданию уже ушло.\n\nСсылка вместо вложения — здравое улучшение, но отдельной задачей. Duration — вкусовщина. А про void неверно: такой метод завершает задание автоматически после успешного выхода."
},

{
  id: "rv-api-field-rename", t: "Design",
  ctx: "Привели имена полей в ответе публичного API к единому стилю.",
  code: "public record OrderView(\n    long id,\n-   String client_name,\n+   String clientName,\n    BigDecimal amount) {}",
  correct: "Переименование поля в публичном ответе ломает всех клиентов без версии API",
  wrong: ["Контракт меняется, а описания в OpenAPI по-прежнему нет: клиенты не видят схему ответа",
          "record для представления — спорный выбор: обычный класс с геттерами привычнее для Jackson и команды",
          "Jackson не умеет сериализовать record: без аннотаций @JsonProperty поля в ответ не попадут вовсе"],
  why: "Внутри сервиса это косметика, снаружи — поломка. Мобильное приложение, партнёрская интеграция, чужой отчёт читают поле client_name; после выката оно исчезнет, и клиенты получат null там, где было имя. Хуже всего то, что упадут они не у тебя, и узнаешь ты об этом из обращений.\n\nПубличный контракт меняют расширением: добавить новое поле рядом со старым, старое пометить устаревшим и убрать через согласованный срок, либо выпускать новую версию API.\n\nOpenAPI — полезно, но не спасает от самой поломки. Спор о record — вкусовщина. А про Jackson неверно: записи он сериализует штатно, начиная с версии 2.12."
},

{
  id: "rv-scheduled-replicas", t: "Infra",
  ctx: "Ежедневная рассылка выписок; сервис работает в трёх репликах.",
  code: "@Scheduled(cron = \"0 0 9 * * *\")\npublic void sendStatements() {\n  for (Client c : clients.active()) {\n    mail.send(c, statements.build(c));\n  }\n}",
  correct: "Задачу стоит закрыть блокировкой: иначе она запустится на всех трёх репликах сразу",
  wrong: ["Рассылка идёт последовательно: на десяти тысячах клиентов она растянется на несколько часов подряд",
          "Крон-выражение лучше вынести в константу с понятным именем",
          "@Scheduled не работает без @Async: метод никогда не вызовется"],
  why: "@Scheduled ничего не знает о соседних экземплярах: у каждой реплики свой планировщик, и в девять утра сработают все три. Для рассылки это тройные письма, для начислений было бы тройное начисление. Это блокирует.\n\nЛечится блокировкой на время выполнения — например, ShedLock поверх базы, — выбором лидера или выносом расписания во внешний планировщик, который дёргает один экземпляр.\n\nПоследовательная рассылка — верное замечание о скорости, его стоит обсудить отдельно. Константа для крона — вкусовщина. А связь с @Async выдумана: @Scheduled работает сам, достаточно @EnableScheduling."
},

{
  id: "rv-liveness-checks-db", t: "DevOps",
  ctx: "Добавили проверку живости для Kubernetes.",
  code: "@GetMapping(\"/live\")\npublic ResponseEntity<Void> live() {\n  jdbc.queryForObject(\"select 1\",\n      Integer.class);\n  return ResponseEntity.ok().build();\n}",
  correct: "Проверку базы стоит перенести в готовность: моргнёт база — и Kubernetes убьёт все поды",
  wrong: ["Запрос к базе на каждую пробу — лишняя нагрузка; результат стоит кэшировать на несколько секунд",
          "Эндпоинт называется не /healthz, хотя так принято в экосистеме Kubernetes и так привычнее дежурным",
          "Проба живости обязана вернуть тело, на пустой 200 под считается мёртвым"],
  why: "Проба живости отвечает на один вопрос: процесс завис, перезапустить его? База сюда не относится. Она моргнула на десять секунд — проба упала у всех подов сразу, Kubernetes убил их одновременно, и сервис, который пережил бы сбой, теперь лежит целиком. А после старта все поды разом прогреваются и открывают соединения, добивая базу.\n\nЗависимости проверяет готовность: неготовый под просто выводится из балансировки и возвращается сам.\n\nКэширование результата — разумно, но для пробы готовности. Имя /healthz — вкусовщина. Про тело ответа неверно: пробе достаточно кода от 200 до 399."
},

{
  id: "rv-index-not-concurrently", t: "DB",
  ctx: "Миграция в релизе: индекс под новый отчёт. В таблице 80 млн строк.",
  code: "-- V31__orders_report_idx.sql\nCREATE INDEX idx_orders_created\n  ON orders (created_at);",
  correct: "Индекс на таблице в 80 миллионов строк стоит строить через CONCURRENTLY: обычный CREATE INDEX блокирует запись на всё время построения",
  wrong: ["Отчёту, скорее всего, нужен составной индекс по статусу и дате, а не по одной дате",
          "Имя индекса не следует шаблону ix_таблица_колонки",
          "Индекс по дате бесполезен: планировщик не использует индексы для условий с диапазоном"],
  why: "В Postgres обычный CREATE INDEX берёт блокировку, которая пропускает чтение, но останавливает вставку, обновление и удаление до конца построения. На восьмидесяти миллионах строк это минуты, и всё это время заказы не принимаются. Это блокирует.\n\nНужен CREATE INDEX CONCURRENTLY. Он не работает внутри транзакции, поэтому миграцию придётся пометить как нетранзакционную, а после сбоя — проверить, не остался ли недостроенный индекс.\n\nСоставной индекс — возможно, верная мысль, но это вопрос к плану запроса. Шаблон имени — вкусовщина. А про диапазоны неверно: B-tree создан как раз для условий «больше» и «между»."
},

{
  id: "rv-unique-in-code", t: "DB",
  ctx: "Регистрация: не даём завести двух клиентов с одним ИИН.",
  code: "if (repo.existsByIin(dto.iin())) {\n  throw new DuplicateClient();\n}\nrepo.save(Client.from(dto));",
  correct: "Здесь стоит завести уникальный индекс: проверка и вставка не атомарны",
  wrong: ["DuplicateClient стоит превращать в ответ 409 с понятным телом, а не отдавать наружу как 500",
          "Маппинг Client.from(dto) живёт внутри самой сущности, а его место — в отдельном маппере проекта",
          "existsByIin вытащит всю таблицу в память: Spring Data реализует exists через findAll и подсчёт"],
  why: "Между проверкой и вставкой есть окно. Клиент дважды нажал кнопку, два запроса одновременно спросили «есть такой ИИН?», оба услышали «нет» и оба вставили. Проверка в коде не защищает ни от чего, кроме последовательных повторов, а дубликаты потом вычищают руками. Это блокирует.\n\nЕдинственная настоящая защита — уникальное ограничение в базе. Код может оставить проверку ради понятной ошибки, но обязан ловить нарушение ограничения и превращать его в тот же ответ.\n\nОтвет 409 — верное замечание про контракт. Отдельный маппер — вкусовщина. А про findAll неверно: exists выполняется запросом с ограничением в одну строку."
},

{
  id: "rv-enum-ordinal", t: "DB",
  ctx: "Добавили статус заявки в сущность.",
  code: "public enum Status { NEW, PAID, SHIPPED }\n\n@Entity class Order {\n  @Enumerated(EnumType.ORDINAL)\n  Status status;\n}",
  correct: "ORDINAL хранит номер по порядку: вставят новый статус в середину — и все старые записи молча сменят смысл",
  wrong: ["Для статуса стоит завести индекс: по нему почти наверняка будут фильтровать заявки в списках и отчётах, а таблица заявок растёт быстрее всех",
          "Перечисление лучше вынести в отдельный файл, а не держать рядом с сущностью",
          "@Enumerated работает только со строковыми колонками, с числовой Hibernate упадёт"],
  why: "ORDINAL пишет в базу 0, 1, 2 — позицию константы в объявлении. Через полгода кто-то добавит CANCELLED между PAID и SHIPPED, и все отгруженные заявки в базе станут отменёнными. Ошибки не будет ни при сборке, ни при запуске: данные просто начнут врать. Это блокирует.\n\nНужен EnumType.STRING или явный конвертер с собственным кодом для каждого значения — тогда порядок в объявлении ничего не значит.\n\nИндекс по статусу — возможно, понадобится, но это отдельный разговор с планом запроса на руках. Отдельный файл — вкусовщина. А про строковые колонки неверно: ORDINAL штатно работает именно с числовой."
},

{
  id: "rv-bulk-import-context", t: "Spring",
  ctx: "Ночной импорт: миллион строк из файла в базу.",
  code: "@Transactional\npublic void importAll(Stream<Row> rows) {\n  rows.forEach(r ->\n      em.persist(Trade.from(r)));\n}",
  correct: "Контекст стоит чистить каждые несколько тысяч строк: иначе миллион сущностей копится в нём до конца",
  wrong: ["Одна транзакция на весь файл: ошибка в последней строке откатит работу за всю ночь, стоит резать на пачки",
          "forEach с побочным эффектом читается как преобразование, хотя это обычный цикл for — так нечестно по отношению к читателю",
          "persist в цикле не сработает: без saveAll сохранится только последняя сущность"],
  why: "Каждая сохранённая сущность остаётся в контексте персистентности до конца транзакции: Hibernate держит её саму и снимок для сравнения. Миллион строк — это миллион объектов и миллион снимков, плюс проверка изменений по всем ним на каждом flush. Импорт сначала замедлится, потом упадёт по памяти. Это блокирует.\n\nЛечится flush и clear каждые несколько тысяч строк вместе с включённым пакетным режимом JDBC, а для таких объёмов — StatelessSession или прямая пакетная вставка.\n\nЗамечание про одну транзакцию верное и близко по важности: его стоит написать рядом. Цикл вместо forEach — вкусовщина. А про saveAll неверно: persist сохраняет каждую сущность."
},

{
  id: "rv-cache-key-no-user", t: "Spring",
  ctx: "Закэшировали портфель, чтобы не считать его на каждый запрос.",
  code: "@Cacheable(\"portfolio\")\npublic Portfolio current() {\n  Long id = SecurityUtils.userId();\n  return calc.forClient(id);\n}",
  correct: "У метода нет аргументов, значит ключ кэша один на всех: первый клиент положит свой портфель, остальные получат чужой",
  wrong: ["У кэша не задано время жизни: портфель устареет после первой же сделки",
          "Имя current слишком общее: из вызова не видно, что речь о портфеле",
          "@Cacheable нельзя ставить на метод без параметров — Spring бросит исключение на старте"],
  why: "Ключ кэша по умолчанию строится из аргументов метода. Аргументов нет — ключ один и тот же для любого вызова. Идентификатор пользователя берётся внутри из контекста безопасности, и кэш о нём не знает: первый вошедший клиент кладёт свой портфель, и дальше его видят все. Это утечка финансовых данных между клиентами, и она блокирует.\n\nНужен ключ, включающий пользователя: передать идентификатор аргументом или задать выражение ключа явно.\n\nВремя жизни — верное и важное замечание, но оно про свежесть, а не про чужие данные. Имя метода — вкусовщина. А про исключение неверно: Spring молча использует пустой ключ, в этом и коварство."
},

{
  id: "rv-idor-no-owner-check", t: "Web",
  ctx: "Эндпоинт для скачивания брокерского отчёта.",
  code: "@GetMapping(\"/reports/{id}\")\npublic Report get(@PathVariable Long id) {\n  return reports.findById(id)\n      .orElseThrow(NotFound::new);\n}",
  correct: "Стоит проверять владельца: перебором id скачиваются чужие отчёты",
  wrong: ["Наружу отдаётся сущность целиком — лучше DTO, чтобы не светить внутренние поля отчёта",
          "Имя NotFound слишком короткое: ReportNotFoundException яснее читается в логах и стектрейсах",
          "Long в @PathVariable небезопасен: нужен String, иначе большие идентификаторы обрежутся"],
  why: "Аутентификация отвечает на вопрос «кто ты», а здесь не задан второй — «твоё ли это». Любой вошедший клиент меняет число в адресе и получает отчёт соседа: идентификаторы идут подряд, перебор занимает минуты. Это самая частая уязвимость в API, и для брокерских данных она блокирует безоговорочно.\n\nНужна проверка, что отчёт принадлежит текущему клиенту, — лучше прямо в запросе: искать по идентификатору и владельцу сразу. На чужой отчёт отвечать 404, а не 403, чтобы не подтверждать его существование.\n\nDTO вместо сущности — верное замечание, стоит написать рядом. Имя исключения — вкусовщина. А про Long неверно: он вмещает идентификаторы без потерь."
},

{
  id: "rv-http-no-timeout", t: "Distributed",
  ctx: "Новый клиент к сервису курсов валют.",
  code: "HttpClient http = HttpClient.newHttpClient();\n\npublic Rate rate(String pair) throws Exception {\n  var req = HttpRequest.newBuilder(uri(pair)).build();\n  var res = http.send(req, BodyHandlers.ofString());\n  return Rate.parse(res.body());\n}",
  correct: "Клиенту стоит задать таймауты на соединение и на ответ: зависнет сервис курсов — и потоки нашего сервиса один за другим повиснут вместе с ним",
  wrong: ["Курсы меняются редко — ответ стоит кэшировать на минуту, а не ходить за ним на каждый запрос",
          "var скрывает типы: с явными типами код читался бы яснее",
          "send() здесь не сработает: синхронный вызов HttpClient разрешён только из главного потока"],
  why: "У HttpClient из JDK таймаутов по умолчанию нет: ни на установку соединения, ни на ожидание ответа. Сервис курсов принял соединение и замолчал — поток ждёт вечно. Следующий запрос занимает следующий поток, и через минуту пул исчерпан: наш сервис лежит из-за чужой поломки. Это блокирует.\n\nНужны оба таймаута: connectTimeout у клиента и timeout у запроса, причём короче, чем таймаут у того, кто зовёт нас.\n\nКэш курсов — здравая мысль, но отдельной задачей. var против явных типов — вкусовщина. А про главный поток выдумано: send вызывается откуда угодно и просто блокирует вызвавший поток."
},

{
  id: "rv-kafka-no-key", t: "Distributed",
  ctx: "Публикуем смену статуса заявки для других сервисов.",
  code: "public void publish(StatusChanged e) {\n  kafka.send(new ProducerRecord<>(\n      \"order-status\", e));\n}",
  correct: "Ключом стоит сделать id заявки: без ключа события разойдутся по партициям, и «исполнена» придёт раньше «создана»",
  wrong: ["Имя топика зашито строкой: его место в конфигурации, чтобы на стендах можно было подставить свой топик без пересборки приложения",
          "ProducerRecord лучше собирать в фабрике, а не прямо в методе",
          "Без ключа Kafka запись не примет: ключ обязателен для любого топика"],
  why: "Kafka хранит порядок только внутри партиции, а партицию выбирает по ключу. Ключа нет — записи раскладываются по партициям как придётся, и два события одной заявки попадают в разные. Потребители читают партиции независимо, так что «исполнена» легко обгоняет «создана», и чужой сервис строит состояние заявки задом наперёд. Это блокирует.\n\nКлючом должен быть идентификатор заявки: все её события лягут в одну партицию и придут по порядку.\n\nИмя топика в конфигурации — верно, но влить не мешает. Фабрика для записи — вкусовщина. А про обязательный ключ неверно: без ключа Kafka запись принимает, в этом и ловушка — всё работает, пока порядок не понадобится."
},

{
  id: "rv-sync-local-lock", t: "Concurrency",
  ctx: "Защитили списание от двойного нажатия. Сервис работает в трёх репликах.",
  code: "public synchronized void withdraw(\n    long accId, BigDecimal sum) {\n  Account a = accounts.get(accId);\n  a.ensureEnough(sum);\n  a.debit(sum);\n  accounts.save(a);\n}",
  correct: "synchronized держит замок внутри одной JVM, а реплик три: два запроса на разных подах спишут оба",
  wrong: ["Замок один на весь сервис: списания по разным счетам встают в общую очередь, хотя друг другу не мешают — пропускная способность упадёт",
          "Параметры названы accId и sum — сокращения читаются хуже, чем accountId и amount, и расходятся с остальным кодом сервиса",
          "synchronized на методе Spring-бина не действует: прокси снимает блокировку"],
  why: "Монитор Java живёт в памяти одного процесса. Балансировщик отправил первое нажатие на один под, второе на другой — каждый взял свой замок, прочитал один и тот же остаток, проверил и списал. Защита выглядит надёжной, проходит тесты на одном экземпляре и не работает ровно там, где нужна. Это блокирует.\n\nИнвариант по деньгам держит база: блокировка строки при чтении, оптимистичная версия с повтором или одно атомарное обновление с условием на остаток.\n\nОбщая очередь для всех счетов — верное замечание, но оно исчезнет вместе с synchronized. Имена параметров — вкусовщина. А про прокси неверно: он вызывает настоящий метод, и synchronized срабатывает как обычно."
},

{
  id: "rv-async-common-pool", t: "Concurrency",
  ctx: "Распараллелили сбор данных для карточки клиента.",
  code: "var profile = CompletableFuture\n    .supplyAsync(() -> crm.profile(id));\nvar limits = CompletableFuture\n    .supplyAsync(() -> risk.limits(id));\nreturn Card.of(profile.join(), limits.join());",
  correct: "Блокирующие вызовы идут в общий ForkJoinPool: он мал и один на всё приложение",
  wrong: ["Ошибка одного из вызовов придёт завёрнутой в CompletionException — её стоит развернуть, чтобы клиент получил осмысленный ответ, а не 500",
          "Два почти одинаковых блока дублируют друг друга: напрашивается один вспомогательный метод async(...)",
          "supplyAsync без явного пула выполняется в вызывающем потоке — параллельности здесь нет вовсе"],
  why: "supplyAsync без второго аргумента берёт общий ForkJoinPool. Потоков в нём — число ядер минус один: на четырёх ядрах это три потока на всё приложение. А на двух ядрах Java 17 и вовсе заводит новый поток на каждую задачу, без всякого предела. Пул рассчитан на короткие вычисления, а сюда приходят сетевые вызовы, которые ждут сотни миллисекунд. Под нагрузкой потоки заняты ожиданием, и встаёт всё, что делит этот пул: параллельные стримы и чужие CompletableFuture по всему приложению. Это блокирует.\n\nНужен собственный ограниченный пул для блокирующих вызовов, переданный вторым аргументом.\n\nРазвернуть CompletionException — верно, но это про качество ответа. Вспомогательный метод — вкусовщина. А про вызывающий поток неверно: задача уходит в поток общего пула."
},

{
  id: "rv-test-no-assert", t: "Testing",
  ctx: "Тест на расчёт комиссии — покрытие выросло до 85%.",
  code: "@Test\nvoid calculatesCommission() {\n  when(rates.forClient(any()))\n      .thenReturn(RATE);\n  service.commission(order);\n  verify(rates).forClient(any());\n}",
  correct: "Тест не проверяет результат: commission может вернуть любое число, хоть ноль, хоть минус, а тест останется зелёным — покрытие есть, проверки нет",
  wrong: ["Случай один: нет граничных — нулевой суммы, минимальной комиссии, клиента без тарифа",
          "Имя теста выбивается из стиля should_..._when_..., принятого в остальных тестах",
          "verify нельзя вызывать после when на том же моке — Mockito бросит исключение"],
  why: "Тест выполняет код, но ничего о нём не утверждает. Результат commission выброшен, а verify подтверждает лишь то, что мок позвали, — то есть пересказывает реализацию. Сломай формулу, поменяй знак, верни ноль — тест зелёный. Хуже отсутствия теста: цифра покрытия говорит «проверено», и следующий разработчик ей поверит. Для расчёта денег это блокирует.\n\nНужна проверка значения: известный тариф, известная сумма, ожидаемая комиссия с точностью до копейки.\n\nГраничные случаи — верное замечание, но сначала должен появиться хотя бы один настоящий. Стиль имени — вкусовщина. А про Mockito неверно: when и затем verify на одном моке — обычная связка."
},

{
  id: "rv-findall-no-page", t: "Web",
  ctx: "Эндпоинт истории сделок для личного кабинета.",
  code: "@GetMapping(\"/trades\")\npublic List<TradeView> mine() {\n  return trades.findByClientId(me()).stream()\n      .map(TradeView::from)\n      .toList();\n}",
  correct: "Выдача ничем не ограничена: у активного клиента сделок сотни тысяч, и один запрос поднимет их все в память",
  wrong: ["Сортировка не задана: порядок сделок будет зависеть от плана запроса и может меняться от вызова к вызову — стоит явно сортировать по дате",
          "Имя mine слишком короткое и непривычное рядом с getMyTrades",
          "Stream.toList() есть только с Java 21, на семнадцатой это не соберётся"],
  why: "Сегодня у тестового клиента двадцать сделок, и всё летает. У алгоритмического клиента их сотни тысяч: один запрос поднимает в память все сущности, потом все представления, потом строку ответа на десятки мегабайт. Несколько таких запросов одновременно — и сервис падает по памяти для всех клиентов. Размер ответа, который растёт вместе с данными, — это отложенная авария, и она блокирует.\n\nНужна постраничная выдача с жёстким верхним пределом размера страницы, для длинной истории — по курсору.\n\nСортировка — верное замечание, и с постраничной выдачей она станет обязательной. Имя метода — вкусовщина. А про Java 21 неверно: Stream.toList() появился в шестнадцатой."
},

{
  id: "rv-trust-role-header", t: "Web",
  ctx: "Админский эндпоинт для смены лимитов клиента.",
  code: "@PostMapping(\"/admin/limits\")\npublic void set(\n    @RequestHeader(\"X-Role\") String role,\n    @RequestBody LimitDto dto) {\n  if (!\"ADMIN\".equals(role))\n    throw new Forbidden();\n  limits.apply(dto);\n}",
  correct: "Роль берётся из заголовка, который клиент пишет сам: X-Role: ADMIN пришлёт кто угодно",
  wrong: ["Смена лимитов никак не журналируется: для таких операций нужен аудит — кто, когда, кому и с какого значения на какое поменял",
          "Строка \"ADMIN\" записана литералом: без константы или перечисления ролей такие литералы расползутся по коду",
          "\"ADMIN\".equals(role) упадёт с NullPointerException, если заголовка нет"],
  why: "Заголовок запроса — это ввод пользователя. Даже если сейчас его проставляет шлюз, сервис доступен внутри сети напрямую, а шлюз может и не вычищать заголовок, пришедший снаружи. Одна строка в curl — и любой меняет лимиты любому клиенту. Это блокирует без обсуждения.\n\nРоль берут из того, что клиент подделать не может: из проверенного токена. В стеке с Keycloak это роли из подписанного JWT и декларативная проверка на методе.\n\nАудит — верное и важное замечание, его стоит написать рядом, но авторизацию он не заменит. Константа для роли — вкусовщина. А про исключение неверно: equals на литерале спокойно принимает null и возвращает false."
},

{
  id: "rv-otp-no-attempt-limit", t: "Web",
  ctx: "Вход по коду из СМС: проверка четырёхзначного кода.",
  code: "@PostMapping(\"/otp/verify\")\npublic Token verify(@RequestBody OtpDto dto) {\n  if (!otp.matches(dto.phone(), dto.code()))\n    throw new WrongCode();\n  return tokens.issue(dto.phone());\n}",
  correct: "Попытки стоит ограничить: четыре цифры перебираются за минуты",
  wrong: ["WrongCode стоит отдавать как 401 с единым телом ошибки, а не как 500 по умолчанию",
          "Имя verify слишком общее: по стектрейсу не понять, что проверяется именно код из СМС",
          "@RequestBody без @Valid не работает: Spring не станет разбирать тело запроса"],
  why: "У четырёхзначного кода десять тысяч вариантов. Без счётчика попыток скрипт перебирает их за несколько минут и получает токен чужого клиента, зная только номер телефона. Код из СМС защищает ровно настолько, насколько ограничено число попыток, — без ограничения это пароль из четырёх цифр. Это блокирует.\n\nНужен счётчик на код и на номер: после трёх–пяти ошибок код сгорает, повторная отправка — с нарастающей паузой, плюс ограничение частоты на сам эндпоинт.\n\nКод ответа 401 — верное замечание про контракт. Имя метода — вкусовщина. А про @Valid неверно: @RequestBody разбирает тело всегда, @Valid лишь добавляет проверку полей."
},

{
  id: "rv-password-fast-hash", t: "Crypto",
  ctx: "Регистрация: перестали хранить пароль открытым текстом.",
  code: "String hash = DigestUtils\n    .sha256Hex(dto.password());\nuser.setPasswordHash(hash);\nusers.save(user);",
  correct: "Для паролей лучше взять bcrypt или Argon2: SHA-256 без соли считается миллиардами в секунду, и после утечки базы пароли подберут на видеокарте",
  wrong: ["Нет проверки сложности пароля: короткие и словарные проходят до хеширования как есть",
          "Хеширование лучше вынести в отдельный PasswordService, а не держать в регистрации",
          "SHA-256 обратим: зная алгоритм, пароль восстанавливают из хеша напрямую"],
  why: "SHA-256 создан быть быстрым — для паролей это недостаток. Видеокарта считает миллиарды хешей в секунду, и типичный пароль подбирается перебором за часы. Соли нет, значит одинаковые пароли дают одинаковый хеш: их видно прямо в таблице, и работают готовые радужные таблицы. Шаг от открытого текста сделан, но в сторону ложного спокойствия. Это блокирует.\n\nДля паролей нужны намеренно медленные функции с солью и настраиваемой стоимостью: bcrypt, scrypt, Argon2. В Spring это готовый PasswordEncoder.\n\nПроверка сложности — верное замечание, отдельной задачей. Отдельный сервис — вкусовщина. А про обратимость неверно: хеш не разворачивается, его подбирают перебором — и беда именно в скорости перебора."
},

{
  id: "rv-xmx-over-limit", t: "DevOps",
  ctx: "Сервису не хватало памяти — подняли кучу.",
  code: "# deployment.yaml\nresources:\n  limits:\n    memory: 2Gi\nenv:\n  - name: JAVA_TOOL_OPTIONS\n    value: \"-Xmx4g\"",
  correct: "Кучу стоит держать ниже лимита контейнера: иначе под нагрузкой под убьёт ядро — без OutOfMemoryError и без дампа",
  wrong: ["Нет -XX:+HeapDumpOnOutOfMemoryError и пути для дампа: при следующей нехватке памяти снова будет не по чему разбираться",
          "JAVA_TOOL_OPTIONS зашита прямо в манифест, а её место в ConfigMap",
          "JVM сама читает лимит контейнера и молча урежет -Xmx до него — падений не будет"],
  why: "JVM разрешили вырасти до четырёх гигабайт, а контейнеру — до двух. Как только процесс перешагнёт лимит, ядро убьёт его: код выхода 137, в событиях пода OOMKilled. Это не OutOfMemoryError — JVM ничего не успеет: ни исключения, ни дампа, ни строчки в логе. Снаружи это выглядит как случайные перезапуски под нагрузкой. Это блокирует.\n\nКуча должна быть заметно меньше лимита: кроме неё память едят метаданные классов, стеки потоков, буферы и сам сборщик. Обычно кучу задают долей от лимита через MaxRAMPercentage и поднимают вместе с лимитом.\n\nДамп при нехватке памяти — полезно, но этот случай он не поймает. ConfigMap — вкусовщина. А про урезание неверно: JVM учитывает лимит только для значений по умолчанию, явный -Xmx она берёт как есть."
},

{
  id: "rv-lombok-data-entity", t: "DB",
  ctx: "Новая сущность клиента со списком заявок.",
  code: "@Data\n@Entity\npublic class Client {\n  @Id @GeneratedValue Long id;\n  String name;\n  @OneToMany(mappedBy = \"client\")\n  List<Order> orders;\n}",
  correct: "@Data строит toString и hashCode по всем полям: строка лога с клиентом потянет из базы все его заявки",
  wrong: ["У связи не задан каскад и orphanRemoval: стоит решить явно, что происходит с заявками при удалении клиента, а не оставлять по умолчанию",
          "Поля без модификатора доступа: в остальных сущностях проекта везде private, а здесь код полагается на доступ из пакета",
          "@Data несовместима с @Entity: Hibernate не запустится с сеттерами Lombok"],
  why: "@Data — это ещё и toString, equals и hashCode по всем полям, включая ленивую коллекцию. Написал в лог клиента — Hibernate пошёл в базу за всеми заявками, а вне транзакции упал с LazyInitializationException. Если у заявки тоже @Data и ссылка на клиента, toString уходит в бесконечную рекурсию до StackOverflowError. А hashCode, зависящий от id, меняется после сохранения, и сущность теряется в HashSet. Три мины одной аннотацией — это блокирует.\n\nДля сущностей берут @Getter и @Setter, а equals и hashCode пишут руками по идентификатору или бизнес-ключу.\n\nКаскад — верное замечание, стоит решить явно. Модификатор private — вкусовщина. А про несовместимость неверно: Hibernate запустится без жалоб, потому такой код и доживает до прода."
},

{
  id: "rv-presigned-url-logged", t: "Infra",
  ctx: "Выдаём клиенту ссылку на скан паспорта из MinIO.",
  code: "String url = minio.getPresignedObjectUrl(\n    GetPresignedObjectUrlArgs.builder()\n        .method(Method.GET)\n        .bucket(\"kyc\").object(key)\n        .expiry(7, TimeUnit.DAYS)\n        .build());\nlog.info(\"ссылка для {}: {}\", clientId, url);\nreturn url;",
  correct: "Ссылка — это ключ к паспорту: она живёт неделю и лежит в логах",
  wrong: ["Имя бакета зашито в коде: его стоит вынести в конфигурацию, на стендах бакеты разные",
          "Построение аргументов лучше вынести в отдельный метод — цепочка слишком длинная",
          "Presigned-ссылки работают только для публичных бакетов, на приватном MinIO вернёт 403"],
  why: "Подписанная ссылка — это пропуск на предъявителя: кто её держит, тот и скачивает, без входа и без проверки владельца. Неделя — максимальный срок, который вообще допускает подпись. И эта ссылка пишется в лог, который читают дежурные, подрядчики и система сбора логов. Получается недельный доступ к паспорту клиента у всех, у кого есть доступ к логам. Это блокирует.\n\nСрок жизни — минуты: ссылка выдаётся под конкретное скачивание. В лог идёт факт выдачи и ключ объекта, но не сама ссылка с подписью.\n\nИмя бакета в конфигурации — верно, но влить не мешает. Отдельный метод — вкусовщина. А про публичные бакеты неверно: подписанные ссылки придуманы как раз для приватных."
}

];
